# In production, frontend is served by the backend (same-origin)
# so no VITE_API_URL is needed. API calls use relative paths like /api/v2/...

# For local dev, Vite proxy handles /api -> localhost:5000 (see vite.config.js)